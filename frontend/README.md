# Frontend

Standalone React frontend for the AI calendar planner proposal.

## Run

```bash
npm install
npm run dev
```

## Notes

- All frontend work is isolated inside `frontend/`.
- Current UI uses mock data so it can be connected to your backend later without blocking your backend work.
- Replace the mock data in `src/data.js` or introduce an API client when your backend endpoints are ready.
